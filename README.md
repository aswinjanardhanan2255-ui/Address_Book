# Address_Book
This is a simple Address Book program written in C. It allows the user to add, search, edit, delete, and view contacts. The contacts are stored in an array and saved in a CSV file. It also checks valid phone numbers and email and avoids duplicate entries.
